# Build 41 — Desktop Hero Animation Reliability

- Desktop hero movement now uses the animated WebP as an absolute full-section image layer.
- Desktop movement no longer depends on browser video autoplay permission.
- iPhone/iPad retain the H.264 mobile video used for Safari compatibility.
- The artwork remains seamless with no border, radius, shadow, or separate panel.
- Cache-distinct stylesheet, script, image, and video references use `v=41`.
