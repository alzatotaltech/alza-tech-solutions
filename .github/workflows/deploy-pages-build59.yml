name: Deploy ALZA Next.js site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAGES: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      # Do not pass static_site_generator: next here.
      # ALZA uses a custom static-export build target in next.config.mjs.
      # Asking configure-pages to rewrite Next config causes its parser to fail
      # on the conditional configuration used by this project.
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Type check
        run: npm run typecheck

      - name: Build static export
        run: npm run build:static

      - name: Verify deploy artifact
        shell: bash
        run: |
          test -f out/index.html
          test -f out/products/index.html
          test -f out/solutions/index.html
          test -f out/pricing/index.html
          test -f out/about/index.html
          test -f out/contact/index.html
          test -f out/sitemap.xml
          test -f out/robots.txt
          echo "Verified Next.js App Router static export files:"
          find out -maxdepth 3 -type f | sort | head -120

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
