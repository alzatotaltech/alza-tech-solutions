# Build 39 — Full-section seamless Home hero

- Renders the animated cloud WebP as a full hero background.
- Removes the separate `<figure>` from visual layout without deleting source markup.
- Eliminates the visible rectangular image canvas.
- Preserves the animated WebP and responsive navigation/grid corrections.
- Uses desktop, tablet and phone-specific background positioning.
